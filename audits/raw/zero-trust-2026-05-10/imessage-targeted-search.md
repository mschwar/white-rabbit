# Targeted iMessage Search Note

**Database:** `~/Library/Messages/chat.db`
**Mode:** targeted keyword count only; no transcript dump.

Query terms:

- `White Rabbit`
- `Scotty`
- `ZoomInfo`
- `GPT`
- `Arizona`
- `VoIP`

Result:

```text
chat_db_readable=yes
matching_product_keyword_messages=0
```

Known handle checks included likely Lee phone evidence from email signature plus a high-count candidate handle. The same targeted product terms returned no grouped hits.

Audit interpretation: iMessage did not add usable product evidence in this run. This is an evidence-access limitation, not proof that no relevant operator messages exist.
